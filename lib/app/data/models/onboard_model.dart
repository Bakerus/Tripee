class OnboardModel {
  final String image, title, description, arguments;
  OnboardModel(
      {required this.image,
      required this.title,
      required this.description,
      required this.arguments});
}
